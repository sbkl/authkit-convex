# Deployment used by `npx convex dev`
CONVEX_DEPLOYMENT=dev:your-convex-slug

NEXT_PUBLIC_CONVEX_URL=https://your-convex-slug.convex.cloud

WORKOS_COOKIE_PASSWORD="" # used by workos to decrypt your session cookie run this command in your terminal openssl rand -base64 24 | tr -dc 'a-zA-Z0-9' | head -c 32
WORKOS_API_KEY=""
WORKOS_CLIENT_ID=""

RESEND_API_KEY=""
EMAIL_FROM=""